/**
 * @file 监控时的全局信息，每次监控都会携带的
 * @author Leo Wang(wangkemiao@baidu.com)
 */

define(function (require) {

    /**
     * 监控时的全局信息，每次监控都会携带的
     * @type {Object}
     */
    var globalData = {
        storageKey: 'fc-monitor-storage-key'
    };

    return globalData;
});
